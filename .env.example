# No environment variables are required for this blank starter.
#
# Keep production secrets in Cloudflare:
# bunx wrangler secret put NAME
#
# Only use VITE_ variables for values that should be public in browser code.
